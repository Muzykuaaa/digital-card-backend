# PostgreSQL connection used by the app, Prisma CLI and seed.
# For local runs without Docker, adjust host/user/password as needed.
DATABASE_URL="postgresql://card:card@localhost:5432/card?schema=public"
